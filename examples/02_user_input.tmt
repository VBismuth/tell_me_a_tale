--- Working with user input ---
There is a "Number Input" of kind number that should listen to me.
There is a "Message Input" that should listen to me.

-- Printing it
Say `Number Input: ` and the meaning of "Number Input".
-- String interpolation is quite verbose with a "the meaning of" keyword, we can use $.identifier.
Say `Message Input: $"Number Input"`.
